# Быстрый старт: Восстановление проекта Кудач

## 📋 Краткая сводка

Проект "Кудач" - это PWA для управления событиями с интеграцией VK. Текущая проблема: **не работает OAuth авторизация через ВКонтакте** из-за изменений в VK API.

## 🎯 Стратегия восстановления

### Этап 1: Исправление VK OAuth (2-3 дня) ⚡ ПРИОРИТЕТ
Сначала восстанавливаем работоспособность на текущей Firebase инфраструктуре.

### Этап 2: Миграция на Yandex Cloud (10-14 дней)
После стабилизации переезжаем на Yandex Cloud.

## 🚀 Что делать прямо сейчас

### Шаг 1: Изучите документацию
2. Откройте [`MIGRATION_PLAN.md`](./MIGRATION_PLAN.md) - полный план миграции на Yandex Cloud

### Шаг 2: Подготовьте окружение
```bash
# Установите зависимости
yarn install

# Установите зависимости для функций
cd functions
yarn install
cd ..

# Проверьте, что Firebase CLI установлен
firebase --version
```

### Шаг 3: Исправьте VK OAuth

#### 3.1 Создайте файл с PKCE утилитами
Создайте `src/utils/pkce.ts` (код в VK_OAUTH_FIX.md)

#### 3.2 Обновите LoginButton.vue
Добавьте генерацию code_verifier и code_challenge (код в VK_OAUTH_FIX.md)

#### 3.3 Обновите Firebase Function
Измените `functions/src/index.ts` для поддержки code_verifier (код в VK_OAUTH_FIX.md)

#### 3.4 Обновите версию VK API
Замените `v=5.101` на `v=5.199` в:
- `src/vk.ts`
- `functions/src/index.ts`

### Шаг 4: Протестируйте локально
```bash
# Терминал 1: Запустите dev-сервер
yarn dev

# Терминал 2: Запустите Firebase emulator
firebase emulators:start --only functions

# Откройте http://localhost:8088 и попробуйте войти
```

### Шаг 5: Деплой в production
```bash
# Деплой функций
firebase deploy --only functions

# Деплой фронтенда
yarn deploy
```

## 📊 Текущий статус задач

✅ **Выполнено:**
- Анализ проблемы VK OAuth
- Исследование требований VK API (PKCE)
- Проектирование архитектуры Yandex Cloud
- Создание плана миграции

🔄 **В процессе:**
- Обновление VK OAuth flow

⏳ **Ожидает:**
- Тестирование OAuth
- Деплой исправлений
- Миграция на Yandex Cloud

## 🔧 Ключевые изменения

### Что сломалось
- VK теперь требует PKCE (code_verifier + code_challenge)
- Старая версия API v5.101 устарела
- Простой обмен code на token больше не работает

### Что нужно добавить
1. **PKCE утилиты** - генерация code_verifier и code_challenge
2. **Обновленный OAuth flow** - передача code_challenge в VK
3. **Обновленная функция** - передача code_verifier при обмене токена
4. **Новая версия API** - v5.199 вместо v5.101

## 📁 Структура проекта

```
kudach/
├── src/
│   ├── components/        # Vue компоненты
│   │   └── LoginButton.vue  # 🔴 Требует изменений
│   ├── store/            # Vuex модули
│   │   └── auth.ts       # Логика авторизации
│   ├── utils/            # Утилиты
│   │   └── pkce.ts       # 🆕 Нужно создать
│   ├── vk.ts             # 🔴 Обновить версию API
│   └── db.ts             # Firebase конфигурация
├── functions/
│   └── src/
│       └── index.ts      # 🔴 Обновить authvk функцию
├── MIGRATION_PLAN.md     # 📖 Полный план миграции
├── VK_OAUTH_FIX.md       # 📖 Инструкция по OAuth
└── QUICK_START.md        # 📖 Этот файл
```

## 🎓 Полезные ссылки

### VK API
- [VK OAuth Documentation](https://dev.vk.com/ru/api/access-token/authcode-flow-user)
- [VK API Methods](https://dev.vk.com/ru/method)
- [PKCE Specification](https://datatracker.ietf.org/doc/html/rfc7636)

### Yandex Cloud (для этапа 2)
- [Yandex Database (YDB)](https://cloud.yandex.ru/docs/ydb/)
- [Cloud Functions](https://cloud.yandex.ru/docs/functions/)
- [Object Storage](https://cloud.yandex.ru/docs/storage/)
- [API Gateway](https://cloud.yandex.ru/docs/api-gateway/)

### Firebase (текущий стек)
- [Firebase Functions](https://firebase.google.com/docs/functions)
- [Firestore](https://firebase.google.com/docs/firestore)
- [Firebase Auth](https://firebase.google.com/docs/auth)

## ⚠️ Важные замечания

1. **Не удаляйте старый код сразу** - сначала убедитесь, что новый работает
2. **Тестируйте на локальном окружении** перед деплоем в production
3. **Сохраните бэкап Firebase** перед миграцией на Yandex Cloud
4. **Обновите VK App настройки** если нужно (redirect_uri)

## 🆘 Если что-то пошло не так

### OAuth не работает
1. Проверьте консоль браузера на ошибки
2. Проверьте логи Firebase Functions
3. Убедитесь, что code_verifier передается корректно
4. Проверьте настройки VK приложения

### Функция не деплоится
1. Проверьте `functions/package.json` на ошибки
2. Убедитесь, что все зависимости установлены
3. Проверьте права доступа Firebase

### Локальное тестирование не работает
1. Убедитесь, что emulator запущен
2. Проверьте proxy настройки в `vite.config.ts`
3. Проверьте порты (8088 для dev, 5001 для emulator)

## 📞 Следующие шаги

После успешного исправления OAuth:

1. ✅ Проверьте все функции приложения
2. 📝 Задокументируйте изменения
3. 🎯 Начните подготовку к миграции на Yandex Cloud
4. 🔍 Изучите документацию Yandex Cloud сервисов
5. 🏗️ Создайте тестовый проект в Yandex Cloud

## 💡 Рекомендации

- **Используйте Git** для версионирования изменений
- **Создайте ветку** для OAuth исправлений
- **Тестируйте каждое изменение** отдельно
- **Документируйте проблемы** для будущей отладки
- **Не спешите** с миграцией на Yandex Cloud - сначала стабилизируйте OAuth

---

