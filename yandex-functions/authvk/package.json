{
  "name": "authvk",
  "version": "1.0.0",
  "description": "VK OAuth function for Yandex Cloud",
  "main": "index.js",
  "scripts": {
    "build": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "engines": {
    "node": ">=18"
  }
}
